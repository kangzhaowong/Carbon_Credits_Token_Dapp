name: leaf_dapp_admin
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^1.0.4
  http: ^0.13.4
  web3dart: ^2.4.0
  web_socket_channel: ^2.2.0
  cupertino_icons: ^1.0.6
  uuid: ^3.0.7
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - src/contracts/Leaf.json
